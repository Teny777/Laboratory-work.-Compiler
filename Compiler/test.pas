program Evklid;
{qjgkfdsafd}
    var 
        M, N,A,M :integer;
        A, D :boolean;
        X,Y,A: double;
    begin
        N :=-3.32;
        X :=3;
        X := (4+N) * X+32.2+5+'hello'+3;
        Y := (N+M)*3.2+(N div M)+X;
        while M <> C do
        begin
            if M>N
            then M := M-'hello'
            else N := N - M
        end;
        if N>A then N:=4;
        if D then N:=5;
        if N then N:=3;
 end.